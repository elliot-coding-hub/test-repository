name: Test

on:
  pull_request:
    types:
      - closed

jobs:
  task_done:
    if: github.event.pull_request.merged
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set the task as Done
      run: |
        sudo sh ./scripts/finish_task.sh
